# autocompletion
## microk8s including kubectl is installed, thats why a 'sudo snap alias microk8s.kubectl kubectl' was needed to get the autocompletion on kubectl.
## to undo the snap alias type 'sudo snap unalias kubectl' to see them 'snap aliases'

